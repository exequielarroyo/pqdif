<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Window
    x:Class="WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="using:Data.Models"
    mc:Ignorable="d"
    Activated="load">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button x:Name="myButton" Click="myButton_Click">Click Me</Button>
        
        <ListView ItemsSource="{x:Bind Blogs}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local1:Blog">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Url}"/>
                        <TextBlock Text="{x:Bind BlogId}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <TextBox x:Name="textBox" PlaceholderText="Url" Text="{x:Bind Blog.Url}"/>
        <TextBlock Text="{x:Bind Blog.Url}"/>

    </StackPanel>
</Window>
